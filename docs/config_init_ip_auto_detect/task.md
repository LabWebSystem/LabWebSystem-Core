# タスク

## 目的
`yarn config:init` の設定ウィザードで、固定IP (`192.168.11.224` など) に依存せず、実行中マシンの IPv4 を選択して設定できるようにする。

## 要求
- プロファイル選択後に、マシンの IPv4 候補を提示する。
- IPv4 が複数ある場合は選択式にする。
- `本番` / `ローカル` を問わず、適用前に確認ダイアログを出す。
- 確認 OK の後にウィザード本体へ進める。
- 適用時は `LAB_CORE_MAIN_SERVICE_IP` と `LAB_CORE_SSH_SERVICE_IP` に反映する。
