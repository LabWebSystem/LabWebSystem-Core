# 実装計画

1. `useDashboardWorkspace` の状態更新経路を確認し、同一レイアウトでも `setDashboard` が走る箇所を特定する
2. `repairDashboard` とドラッグ終了処理に、内容が変わらないときは既存 state を再利用するガードを入れる
3. 保存処理にレイアウト署名ベースの重複送信抑止を追加し、初期ロード直後や即時保存と自動保存の二重送信を減らす
4. `yarn build` で確認し、作業ログを保存してコミットする
