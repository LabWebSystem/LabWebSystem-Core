# タスク

## 背景

- ダッシュボードでレイアウト保存時に `PUT /api/system/dashboard-layout` が無数に発生する
- ブラウザ側では `net::ERR_INSUFFICIENT_RESOURCES` が出て保存に失敗する

## 目的

- レイアウト保存の無限送信を止める
- 同一内容の重複保存を抑える
- 既存のレイアウト編集操作は維持する

## 完了条件

- レイアウト未変更時に保存 API が連打されない
- 画面初期表示や編集終了時に同一ペイロードの重複保存が起きにくい
- `core/dashboard` のビルドが通る
