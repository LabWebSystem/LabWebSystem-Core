# 実装計画

## 方針

前回修正後も残った症状に対して、表示条件の改善だけでなく、リサイズ制約の永続化バグとドラッグ中プレビュー不足を重点的に直す。

## 実施項目

1. 編集モード専用だった全削除ボタンを、通常時にも存在が見える形へ変更する
2. ガードレール修復を高さ変化やページ切り替えでも再実行できるようにする
3. ウィジェット定義由来ではない `maxH/maxW` が保存されて拡張不能になる挙動を除去する
4. ドラッグ中のウィジェットを固定オーバーレイでカーソル追従表示する
5. ビルド確認を行う
