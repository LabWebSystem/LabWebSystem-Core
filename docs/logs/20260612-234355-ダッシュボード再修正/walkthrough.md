# 修正内容の確認

## 実施内容

- `全ウィジェット削除` ボタンを常時表示に変更し、通常時は無効化して存在が分かるようにした
- ガードレール修復は違反があるときだけ再実行するようにしつつ、ページ切り替えや `maxRows` 変化にも追従できるようにした
- サイズ補正ロジックで、過去のドラッグやリサイズから持ち越された `maxH/maxW` を信頼しないようにして、定義ベースの上限へ戻した
- ドラッグ中はオーバーレイのプレビューウィジェットを表示し、ページ移動時もカーソルに追従する見た目を補った

## 確認結果

- 実行コマンド:
  `yarn --cwd core/dashboard vite build --outDir dist-codex-check`
- 結果:
  ビルド成功
- 補足:
  ブラウザの実機確認まではこの場では未実施
