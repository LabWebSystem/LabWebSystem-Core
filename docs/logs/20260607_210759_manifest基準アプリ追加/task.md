# タスク

1. ダッシュボードのアプリ追加画面で、GitHub URL 解析時に `labcore.app.yaml` だけを探す。
2. `docker-compose.yaml` を含む YAML 全般の候補探索をやめ、`labcore.app.yaml` の内容を初期値として登録フォームへ反映する。
3. `labcore.app.yaml` が存在しない場合はエラーにする。
4. backend / dashboard のビルドと関連テストで動作確認する。
