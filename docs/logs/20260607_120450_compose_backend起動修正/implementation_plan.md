# 実装計画

1. ルート `package.json` と Compose 定義を確認し、`dev:backend` の定義有無と参照箇所を特定する。
2. 実行時に使われる旧コマンド参照を、現行コマンド `service:backend:up` に置き換える。
3. 影響範囲として、backend 起動に依存するスモークテストスクリプトも同じ方針で更新する。
4. `docker compose` の設定解釈、シェル構文、Compose 起動、HTTP ヘルスチェックで修正を検証する。
5. 変更内容と検証結果をログとして保存し、適切な粒度でコミットする。
