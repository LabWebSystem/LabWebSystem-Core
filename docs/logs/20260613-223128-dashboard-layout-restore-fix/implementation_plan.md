# 実装計画

1. ダッシュボード表示直後に `repairDashboard()` と `endWidgetDrag()` が走る条件を見直し、測定未完了時の整形を止める。
2. 現在ページのレイアウトが本当にグリッド制約違反のときだけ修復するガードを追加する。
3. 編集モードの終了時だけ保存や整形を確定させ、初期表示や通常表示での不要な再整形を止める。
4. `wheel` ハンドラを非 passive なネイティブリスナーへ移し、コンソール警告を解消する。
5. 型チェックと別出力先ビルドで動作確認する。
