# 実装計画

1. `HomeView` に残っている責務を、レイアウト管理・メトリクス取得・ログ取得・ウィジェット描画・追加モーダルに分解する
2. レイアウト定義やウィジェット定義などの純粋ロジックを `src/dashboard` へ移す
3. 複雑な状態管理を `src/hooks` のカスタムフックへ移す
4. 各ウィジェットを `src/widgets/dashboard` の個別コンポーネントへ分離する
5. `HomeView` を画面組み立て中心の薄いファイルへ置き換える
6. ビルド確認を行い、参照文書に新しい構成を追記する
