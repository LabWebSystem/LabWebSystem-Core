# タスク

作成日: 2026-06-07

## 依頼
1. SSH 統合手順を Phase3 の計画どおり実行する。
2. 追加の SSH セキュリティ設定強化は今回は行わない。
3. docs の配置ルールを次の方針へ整理する。
   - 説明書は `docs/readmes`
   - 一時ログは `docs/logs`
   - logs 統合要約は `docs/summarys`
   - 統合前ログは `docs/archives`
   - 草案は `docs/temps`
4. このルールを `AGENTS.md` に追加する。
5. `docs/readmes` のファイル名は日本語へ統一する。

## 完了条件
- repo 側の設定・説明書・運用ルールが新方針に揃っている。
- SSH 統合に必要な repo 側の値が `192.168.40.224` に揃っている。
- 実機切替で権限不足がある場合は、その事実と未完了点が記録されている。
