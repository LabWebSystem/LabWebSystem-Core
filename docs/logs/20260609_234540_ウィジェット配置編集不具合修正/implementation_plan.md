# 実装計画

1. ダッシュボードのレイアウトユーティリティに、既存ウィジェットとの衝突を避けながら空きスロットを探索する処理を追加する
2. 新規ウィジェット追加処理を、現在ページから順に配置可能ページを探索し、見つからなければ末尾ページを追加して配置する方式へ変更する
3. HomeView 側で編集モード OFF 時のグリッドを完全に非インタラクティブ化し、ドラッグ中状態を解除する
4. ダッシュボードのビルドを実行して回帰がないことを確認する
