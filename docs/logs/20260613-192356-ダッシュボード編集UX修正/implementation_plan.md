# 実装計画

1. 編集 UI の現状を確認し、削除ボタン・リサイズハンドル・追加ボタンの表示条件を整理する。
2. `HomeView` の interaction 終了処理と `useDashboardWorkspace` の保存経路を見直し、自動保存と編集完了時保存の競合を解消する。
3. `moduleAdapter` で重ね配置失敗を例外ではなく結果として返せるようにし、`HomeView` 側でトースト表示へつなぐ。
4. ドラフトページの視認性を上げる表示を追加する。
5. 型確認と別出力先ビルド、重ね配置失敗時の戻り値確認を行う。
