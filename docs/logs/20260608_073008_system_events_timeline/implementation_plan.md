# 実装計画

1. ホーム画面のシステムイベント表示箇所を確認する
2. 直近イベントの並び順を「古い → 新しい」に変える
3. イベント表示をカードグリッドから縦のタイムラインへ変更し、時刻・scope・level・本文の順序が追えるようにする
4. `corepack yarn workspace @lab-core/dashboard build` で表示崩れや型の問題がないことを確認する
