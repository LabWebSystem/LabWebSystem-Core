# タスク整理: SSH / docs/readmes / SDKドキュメント改善計画

作成日: 2026-06-07

## 依頼内容
1. `ssh` が現在待ち受けている IP に対して、`ssh.fukaya-sus.lab` でアクセスできるようにする改善案を出す。
2. 可能であれば、SSH 用の分離 IP を維持せずに `192.168.40.224` 側へ統合し、セキュリティを保ったまま `192.168.40.225` を廃止できるかを検討する。
3. `docs/readmes` のファイル名・役割・内容フォーマットを統一する方針を作る。
4. `docs/readmes` に SDK の仕様と使い方を説明する文書を追加するため、構成案と記載項目を具体化する。

## 今回の調査で重視する観点
- 現行実装にすでにある仕組みを活かせるか
- DNS / SSH / firewall の責務分離が明確か
- 将来の引き継ぎ時に迷わないドキュメント体系になるか
- SDK 文書が「概念説明」と「実装仕様」の両方を満たせるか

## 完了条件
- 3要望それぞれについて、実施順序付きの推奨案がある。
- `docs/readmes` 整理の命名規則と文書テンプレート方針が定義されている。
- SDK 文書に載せるべき具体項目（manifest, profile, CLI, library API, CI 導線）が列挙されている。
