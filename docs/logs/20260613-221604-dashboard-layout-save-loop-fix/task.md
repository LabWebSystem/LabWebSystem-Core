# タスク

## 件名

ダッシュボードのレイアウト保存で `PUT /api/system/dashboard-layout` が大量発生し、`net::ERR_INSUFFICIENT_RESOURCES` になる問題を修正する。

## 背景

- レイアウト編集や表示直後に保存通信が無数に走る。
- ブラウザ側で接続リソースを使い切り、保存失敗が連続する。

## 完了条件

- 同一レイアウトに対する不要な保存通信が連続しない。
- ダッシュボード表示中に保存ループが発生しない。
- フロントエンドの型チェックまたはビルド相当の確認が通る。
