# 実装計画

1. レイアウト保存フローを確認し、無限保存の起点になっている state 更新と effect 依存を特定する。
2. `useDashboardWorkspace` で同一レイアウトの再保存を抑止し、整形後に内容が変わらない場合は state を更新しないようにする。
3. 保存中または保存済みの同一レイアウトに対する重複通信を抑止する。
4. 型チェックと別出力先ビルドで修正後の妥当性を確認する。
