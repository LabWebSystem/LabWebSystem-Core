# 実装計画

1. `tasks` TUI の自前キーハンドリングをやめ、`@inquirer/prompts` の `select` / `search` ベースへ移行する。
2. 既存の階層データ構造（`:` 区切り）を維持し、グループ開閉を `select` 操作で実現する。
3. 「検索して実行」「すべて展開」「すべて折りたたむ」のクイックアクションを追加する。
4. `search` プロンプトに簡易スコアリングを導入し、コマンド名・実行文字列の両方で絞り込めるようにする。
5. 型チェックと実機 TTY 起動で UX を確認する。
