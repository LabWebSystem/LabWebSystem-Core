# タスク

`package.json` の scripts が肥大化しているため、以下を実施する。

- 各 script の役割を列挙する。
- 実運用で本当に必要かを判定する。
- 可能な範囲で実行ロジックを `package.json` から切り出し、管理しやすくする。

## 完了条件

- 既存 script 名を維持しつつ、実行ロジックが外部スクリプトに集約されている。
- script ごとの必要性（必須/運用補助/互換）が整理されている。
