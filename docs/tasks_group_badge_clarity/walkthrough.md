# 修正内容の確認

## 1. 表示改善

- グループ行の `run` 表示を削除。
- 代わりに `cmd` バッジを表示し、「このグループは実行コマンドを持つ」ことを示すよう変更。

## 2. 実行導線の明確化

- グループ選択時の詳細エリアに、次を表示:
  - `このグループ自体もコマンドです。展開後の ▶ <group> を選んで Enter`

## 3. 検証

- `yarn run typecheck:scripts` 成功。
- `yarn run tasks` のTTY表示で、`run` 表示がなくなり、`cmd` バッジと実行案内が表示されることを確認。
