# 実装計画

1. `interactive-tasks.ts` にANSIスタイルユーティリティを追加する。
2. ヘッダーをカード風に再構成し、状態チップ（commands/groups open）を表示する。
3. 操作キーをキーキャップ風に表示し、視線誘導を改善する。
4. 一覧行に色分け・選択行ハイライト・階層ガイドを導入する。
5. フッターに選択中コマンドの要約を表示する。
6. 既存のキー仕様（`←/→` 開閉、`Enter` 実行専用）を維持したまま検証する。
