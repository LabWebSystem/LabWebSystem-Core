# 実装計画

1. README の冒頭付近に「前提依存関係」セクションを追加する。
2. 必須依存（Node.js, corepack yarn, Docker, Compose, Git）を列挙する。
3. `Node 22` 推奨と、`Node 24` で発生した `better-sqlite3` ビルド失敗の補足を記載する。
4. 必要に応じてホストビルドツール（make / gcc-c++ / python3）要件を補足する。
